# app

blah blah blah

# Run app with docker

```
sudo docker-compose up -d
```

# Run app without docker

```
pip install pipenv
pipenv install
pipenv shell
sudo docker-compose up -d db
fastack runserver
```
